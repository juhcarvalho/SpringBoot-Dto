package hogwarts.repositories;

import hogwarts.domain.models.Bruxo;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BruxoRepository extends JpaRepository<Bruxo, Long> {
}
